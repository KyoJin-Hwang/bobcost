---
title: '📚 자바스크립트 자료형'
desc: 각 자료형 특징과 사용법을 쉽게 이해할 수 있도록 정리했습니다. 
createdAt: 2025-04-05 15:05 
thumbnail: /posts/frontend/javascript/자료형.png
group: JS
---

## ⭐️ 자료형이란?
자바스크립트에서 값은 항상 **문자열**이나 **숫자형** 같은 특정한 자료형에 속합니다.

자바스크립트에는 여덟 가지의 기본 자료형이 있습니다.

자바스크립트의 변수는 자료형에 관계없이 모든 데이터일 수 있습니다. 따라서 변수는 어떤 순간에 문자열일 수 있고 다른 순간엔 숫자가 될 수도 있습니다.

```js showLineNumbers ${1-2}
let message = '안녕 나는 문자열이야'
message = 12345 

console.log(message) 
// 👉 결과: 12345
```
> 이처럼 자료의 타입은 있지만 변수에 저장되는 값의 타입은 언제든지 바꿀 수 있는 언어를 ‘동적 타입(dynamically typed)’ 언어라고 부릅니다.

### 📌 원시 자료형 (Primitive Type)
- 객체가 아니며, 메서드를 가지지 않습니다.  
- 하나의 값만을 저장합니다.  
- 변수에 값 자체가 저장되며, 복사 시 값이 그대로 복제됩니다.  
- 변경이 불가능한(immutable) 값입니다.  

#### 종류(원시)
- `string` : 문자열  
- `number` : 숫자 (정수 및 실수)  
- `boolean` : true 또는 false  
- `null` : 의도적으로 "값이 없음"을 명시  
- `undefined` : 값이 할당되지 않은 변수  
- `symbol` : 고유하고 변경 불가능한 값  
- `bigint` : 아주 큰 정수를 다룰 수 있는 자료형

### 📌 참조 자료형 (Reference Type)
- 객체를 기반으로 하며, 여러 개의 값을 담을 수 있는 구조입니다.  
- 변수에는 값이 아닌 해당 객체의 참조 주소가 저장됩니다.  
- 복사 시 참조값이 복제되어, 두 변수는 같은 객체를 가리키게 됩니다.  
- 변경 가능한(mutable) 데이터입니다.  

#### 종류(참조)
- `object` : 키-값 쌍으로 구성된 데이터 집합  
- `array` : 순서가 있는 값들의 리스트  
- `function` : 재사용 가능한 코드 블록이며, 일급 객체로 취급됨  
- `date` : 날짜와 시간 데이터를 표현하기 위한 객체  
- `regexp` : 정규표현식 처리를 위한 객체  
- `map` : 키-값 쌍의 컬렉션으로, 키에 어떤 타입이든 사용 가능  
- `set` : 중복되지 않는 유일한 값들의 집합

--- 

## 🚀 자료형

### 📌 숫자형 (Number)

- 숫자형(number type) 은 정수 및 부동소수점 숫자(floating point number)를 나타냅니다.
- 숫자형과 관련된 연산은 다양한데, 곱셈 *, 나눗셈 /, 덧셈 +, 뺄셈 - 등이 대표적입니다.
- 숫자형엔 일반적인 숫자 외에 Infinity, -Infinity, NaN같은 '특수 숫자 값(special numeric value)'이 포함됩니다.

```js showLineNumbers
let num = 123456
num = 12.3456
```

**NaN**은 계산 중에 에러가 발생했다는 것을 나타내주는 값입니다. 부정확하거나 정의되지 않은 수학 연산을 사용하면 계산 중에 에러가 발생하는데, 이때 NaN이 반환됩니다.

| 표현식               | 결과            | 이유                                                                 |
|--------------------|----------------|----------------------------------------------------------------------|
| "문자열" ( / , * , - ) 숫자   | `NaN`          | `/`, `*`, `-` 는 숫자 연산을 시도하지만 `"문자열"`은 숫자로 변환할 수 없기 때문에 `NaN` 반환 |
| "문자열" + 숫자   | `"문자열숫자"` | `+`는 문자열 연결 연산자처럼 작동하여 숫자를 문자열로 변환 후 연결함          |

```js showLineNumbers
console.log("숫자가 아님" / 2);
console.log("숫자가 아님" * 2);
console.log("숫자가 아님" - 2);
// 👉 결과: NaN
// 👉 결과: NaN
// 👉 결과: NaN
console.log("숫자가 아님" + 2); 
// 👉 결과: "숫자가 아님2"
```

---

### 📌 문자형 (String)
자바스크립트에선 문자열(string)을 따옴표로 묶습니다.

문자열을 나타내는 방식은 3가지가 있습니다.
- 큰따옴표: "Hello"
- 작은따옴표: 'Hello'
- 역 따옴표(백틱, backtick): `` `Hello` ``

따옴표와 작은따옴표는 ‘기본적인’ 따옴표로, 자바스크립트에서는 이 둘에 차이를 두지 않습니다.
역 따옴표로 변수나 표현식을 감싼 후 `${…}`안에 넣어주면, 아래와 같이 원하는 변수나 표현식을 문자열 중간에 손쉽게 넣을 수 있습니다.

```js showLineNumbers
let str = "Hello!";
let str2 = 'Hello 2!';
let str3 = `Owen ${str}`;
```

--- 

### 📌 불린형 (Boolean)
불린형(논리 타입)은 true와 false 두 가지 값밖에 없는 자료형입니다.
불린형은 긍정(yes)이나 부정(no)을 나타내는 값을 저장할 때 사용합니다. true는 긍정, false는 부정을 의미합니다.

```js showLineNumbers
let yes = true
let no = false

let isGreater = 4 > 3
console.log(isGreater)
// 👉 결과: true
```

---

### 📌 null 값 (null)
`null` 값은 지금까지 소개한 자료형 중 **어느 자료형에도 속하지 않는 값**입니다.
`null` 값은 오로지 `null` 값만 포함하는 **별도의 자료형**을 만듭니다.


```js showLineNumbers
// let age = null; 은 나이(age)를 알 수 없거나 그 값이 비어있음을 보여줍니다.
let age = null;
```

자바스크립트에선 null 사용하는 방법
- ‘존재하지 않는(nothing)’ 값
- ‘비어 있는(empty)’ 값
- ‘알 수 없는(unknown)’ 값을 나타내는 데 사용합니다. 

### 📌 undefined 값 (undefined)
`undefined` 값도 `null` 값처럼 자신만의 자료형을 형성합니다.
`undefined`는 '값이 할당되지 않은 상태’를 나타낼 때 사용합니다.
변수는 선언했지만, 값을 할당하지 않았다면 해당 변수에 `undefined`가 자동으로 할당됩니다.

<CodeRunner
  code={`let age = [1,2,3]
  console.log(age[3])

  let name
  console.log(name)`}
  showLineNumbers
/>

### 📌 객체와 심볼 (Object and Symbol)

`객체 (Object)`
- 특수한 자료형으로, 데이터와 동작을 함께 표현할 수 있는 구조입니다.
- 일반적인 원시형(문자열, 숫자, 불린 등)과 달리, 하나 이상의 값을 그룹으로 묶어서 표현할 수 있습니다.
- 객체는 **속성(key-value 쌍)** 으로 이루어져 있으며, 다양한 데이터를 담을 수 있어 **복잡한 개체(entity)** 를 표현할 때 사용됩니다. 

```js showLineNumbers
let user = {
  name: "Kyojin",
  age: 29
  birth: "1997-11-27"
  gender: "Man"
};
```

`심볼 (Symbol)`
- ES6에 도입된 자료형입니다.
- 고유하고 유일한 식별자를 만들 때 사용됩니다.
- 주로 객체의 속성 키로 사용되며, 다른 코드와 충돌하지 않도록 하는 데 유용합니다.
- 심볼은 동일한 설명을 사용해 생성해도 서로 다른 값입니다.

<CodeRunner
  code={`// 'password'라는 설명을 가진 심볼을 생성 (고유한 값)
let password = Symbol("password");

// 'user' 객체 생성, 심볼 키를 이용해 비밀번호 정보를 감춤
let user = {
    name: "Admin",
    [password]: "secret" // 일반 문자열 키가 아닌, 심볼 키를 사용
};

// 비밀번호를 확인하는 함수
function checkPassword(obj, pwd) {
    // 심볼 키로 저장된 비밀번호와 입력값을 비교
    return obj[password] === pwd;
}

// 함수 호출: 비밀번호가 맞으면 true 반환
console.log(checkPassword(user, "secret"));`}
/>

| 구분             | 객체 (Object)                                            | 심볼 (Symbol)                                           |
|------------------|-----------------------------------------------------------|----------------------------------------------------------|
| 자료형 종류      | 참조형 (Reference Type)                                   | 원시형 (Primitive Type)                                 |
| 목적             | 데이터를 여러 개 묶어 표현 (컬렉션, 개체 등)              | 고유한 속성 키를 만들기 위해 사용                        |
| 사용 용도        | 다양한 속성과 메서드를 가진 복합 데이터 구조              | 객체 속성의 **유일한 키**로 사용                         |
| 생성 방법        | `{}` 또는 `new Object()`                                  | `Symbol()` 함수로 생성                                   |
| 키로 사용 여부   | 일반적으로 키로 사용됨 (`key: value` 형식)               | 객체의 키로 사용 가능하나, 고유하고 숨겨진 키로 활용됨  |
| 고유성           | 객체 자체는 고유하지만 키 이름은 문자열로 중복 가능       | 생성할 때마다 고유하며 절대 중복되지 않음               |
| 비교 가능성      | 참조에 따라 동일 여부 판단 (`===`는 주소 비교)            | 항상 고유 (`Symbol("a") !== Symbol("a")`)               |
| JSON 직렬화      | 가능 (`JSON.stringify`)                                   | 심볼 키는 직렬화되지 않음 (무시됨)                      |
| 상징적 사용 예   | 사용자 정보, 설정, DOM 등 데이터 표현                    | 내부 속성 식별자, 라이브러리 간 충돌 방지                |

![](https://blog.kakaocdn.net/dn/cMlPpJ/btrzzp5xhgK/JH0RKu6urt0mYuekeeXB11/img.gif)