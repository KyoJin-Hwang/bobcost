---
title: '📚 자바스크립트 연산자'
desc: 여러 연산자의 특징과 사용법을 쉽게 이해할 수 있도록 정리했습니다.
createdAt: 2025-04-05 15:10
thumbnail: /posts/frontend/javascript/연산자.png
group: JS
---

## 💡 연산자란 무엇인가요?
연산자는 하나 이상의 값을 이용해 새로운 값을 만드는 문법 요소입니다. 
이때 연산의 대상이 되는 값을 **피연산자(operand)**, 연산을 수행하는 기호나 키워드를 **연산자(operator)** 라고 합니다.

피연산자의 개수에 따라 **단항(Unary)** 과 **이항(Binary)** 으로 나뉘고, 그중 산술 연산자도 단항/이항으로 나뉩니다.

```js
1 + 2   // '+'는 연산자, 1과 2는 피연산자
```

### 📕 수학연산자 종류
- 덧셈 연산자 +
- 뺄셈 연산자 -
- 곱셈 연산자 *
- 나눗셈 연산자 /
- 나머지 연산자 %
- 거듭제곱 연산자 **

<CodeRunner
  code={`const a = 2 + 3;
  const b = 10 - 2
  const c = 10 * 10 
  const d = 20 / 5
  const e = 13 % 4
  const f = 2 ** 3
  
  console.log('덧셈 : ' + a)
  console.log('뺄셈 : ' + b)
  console.log('곱셈 : ' + c)
  console.log('나눗셈 : ' + d)
  console.log('나머지 : ' + e)
  console.log('거듭제곱 : ' + f)
  `}
/>
--- 

## ⭐️ 이항 연산자와 문자열 연결
덧셈 연산자 +는 대개 숫자를 더한 결과를 반환합니다.
그런데 이항 연산자 +의 피연산자로 문자열이 전달되면 덧셈 연산자는 덧셈이 아닌 문자열을 병합(연결)합니다.
<CodeRunner
  code={`// 예시 1
let s = "my" + "string";
console.log(s); // mystring

// 예시 1
console.log( '1' + 2 ); // "12"
console.log( 2 + '1' ); // "21"

// 예시 3
console.log(2 + 2 + '1' ); // '221'이 아니라 '41'이 출력됩니다.`}
/>

<Callout type='danger'> 만약 뺄셈 -과 나눗셈 / 연산자가 어떻게 문자형 피연산자를 다룰까요?</Callout>

<CodeRunner
  code={`const a = ( 6 - '2' ); 
const b = ( '6' / '2' ); 
const c = ( '6' - '2')

console.log(a)
console.log(b)
console.log(c)`}
/>
위 코드와 같이 뺄셈 -과 나눗셈 / 은 문자형을 숫자형으로 변환되어 계산된다.

## 안녕하세요